import UploadForm from "@/components/UploadForm";

export const metadata = {
  title: "Add to BKGrit — Upload",
  description: "Upload a Twitter Space, ingest a Nets podcast from YouTube, or connect an RSS feed.",
};

export default function UploadPage() {
  return <UploadForm />;
}
