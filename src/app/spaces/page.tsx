import SpacesArchive from "@/components/SpacesArchive";

export const metadata = {
  title: "Spaces Archive — BK Grit",
  description: "Post-game fan reactions and discussions. Listen to archived Spaces and relive the moments.",
};

export default function SpacesPage() {
  return <SpacesArchive />;
}
