import KBSubmitForm from "@/components/KBSubmitForm";

export const metadata = {
  title: "Submit a Source — BK Grit KB",
  description: "Drop a link, help build the Nets Wiki. Articles, tweets, scouting reports, trade rumors — anything Nets-related.",
};

export default function KBSubmitPage() {
  return <KBSubmitForm />;
}
